# Configurações de Email (SMTP)
# Para Gmail, você precisa criar uma "App Password" em:
# https://myaccount.google.com/apppasswords
# 
# Copie este arquivo para .env e configure com seus valores

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASSWORD=sua-app-password-gmail
CONTACT_EMAIL=contato@seu-dominio.com

# Ambiente
NODE_ENV=production

# Porta (geralmente definida automaticamente pela Hostinger)
PORT=3000

# Hostname (opcional)
HOSTNAME=localhost

